mod common;

#[test]
fn test_client_connection() {
    common::setup();

    /*
        client_connection(
            ws: WebSocket,
            id: String,
            clients: Clients,
            client: Client,
            games: Games)
    */
}
